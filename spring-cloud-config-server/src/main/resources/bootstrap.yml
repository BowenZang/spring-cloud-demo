spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BowenZang/spring-cloud-server-config
          username: BowenZang@outlook.com
          password: zbw199444.
          search-paths: /config/*
