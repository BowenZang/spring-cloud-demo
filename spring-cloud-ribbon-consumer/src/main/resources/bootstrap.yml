spring:
  application:
    name: ribbon-consumer

server:
  port: 10000

eureka:
  instance:
    namespace: client
  client:
    service-url:
      defaultZone: http://peer1:8000/eureka/, http://peer2:8001/eureka/
