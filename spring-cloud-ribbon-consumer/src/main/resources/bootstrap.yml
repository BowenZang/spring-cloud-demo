spring:
  application:
    name: ribbon-consumer

server:
  port: 10000

eureka:
  instance:
    namespace: client
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
