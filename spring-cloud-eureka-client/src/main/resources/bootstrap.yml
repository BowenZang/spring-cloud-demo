server:
  port: 9000

spring:
  application:
    name: clientA

eureka:
  instance:
    namespace: client1
  client:
    service-url:
      defaultZone: http://peer1:8000/eureka/, http://peer2:8001/eureka/