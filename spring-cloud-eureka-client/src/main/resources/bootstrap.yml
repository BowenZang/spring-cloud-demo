server:
  port: 9000

# 暴漏服务的服务应用名
spring:
  application:
    name: clientA

eureka:
  instance:
    namespace: client1
  client:
    service-url:
      defaultZone: http://peer1:8000/eureka/, http://peer2:8001/eureka/